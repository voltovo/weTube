doctype html 
html(lang="ko")
    head 
        title #{pageTitle} | #{siteName}
        link(rel="stylesheet" href="https://unpkg.com/mvp.css@1.12/mvp.css")
        link(rel="icon" type="image/png" href="http://example.com/myicon.pug")
    body
        header
            h1=pageTitle
            nav
                ul
                    li
                        a(href="/") Home
                    if loggedIn
                        li
                            a(href="/users/logout") LogOut
                        li
                            a(href="my_profile") #{loggedInUser.name}의 PROFILE
                    else
                        li
                            a(href="/join") Join
                        li
                            a(href="/login") LogIn
                    li
                        a(href="/search") Search
                    li
                        a(href="/videos/upload") Upload Video
        main
            block content
    include partials/footer
